import styled from "styled-components";

const TimestampConverterContainer = styled.div`
    margin-top: 10px;
    text-align: center;
    font-size: 14px;
`;

export default TimestampConverterContainer;
